---
import qs from "qs";
import mostPopular from "../assets/mostPopular.svg";
import Pricecut from "../assets/Pricecut.svg";
import priceDiscountIcon from "../assets/priceDiscountIcon.png";
import { Image } from "astro:assets";
const homePageQuery = qs.stringify(
  {
    populate: {
      section7: {
        populate: "*",
      },
    },
  },
  { encodeValuesOnly: true },
);

async function getStrapiData(path: string) {
  const BASE_URL = import.meta.env.PUBLIC_BACKEND_URL;
  const url = new URL(path, BASE_URL);
  url.search = homePageQuery;

  try {
    const res = await fetch(url.href);
    const json = await res.json();
    return json.data;
  } catch (error) {
    console.error("Strapi fetch error:", error);
  }
}

const data = await getStrapiData("/api/home-page");

const section7 = data?.section7?.find(
  (b: any) => b.__component === "layout.section7",
);
---

<section class="pricing-section">
  <div class="pricing-container">
    <p class="pricing-title">
      Find the plan that fits your<br /> learning journey
    </p>

    <p class="pricing-subtitle">
      Short, expert reviewed videos for school, university, work and beyond.
    </p>

    <div class="pricing-row ant-row ant-row-center css-vrrzze">
      <!-- ANNUAL CARD -->
      <div class="pricing-col">
        <Image alt="Popular" src={mostPopular} class="popular-badge" />

        <div class="pricing-card featured-card ant-card ant-card-bordered css-vrrzze">
          <div class="ant-card-body pricing-card-body">
            <div class="pricing-header">
              <div class="pricing-label-row">
                <p class="plan-type">Annual</p>

                <span class="discount-tag ant-tag ant-tag-has-color css-vrrzze">
                  <Image
                    alt="save icon"
                    src={priceDiscountIcon}
                    class="discount-icon"
                  />
                  <span class="discount-text">Save 44%</span>
                </span>
              </div>

              <h3 class="price-text">$16/month</h3>
            </div>

            <Image alt="Pricecut" src={Pricecut} class="pricecut-img" />

            <hr class="card-divider" />

            <ul class="feature-list">
              <li class="feature-item">
                <span class="feature-dot"></span>Unlimited access
              </li>
              <li class="feature-item">
                <span class="feature-dot"></span>One time payment
              </li>
              <li class="feature-item">
                <span class="feature-dot"></span>Cancel anytime
              </li>
              <li class="feature-item">
                <span class="feature-dot"></span>Renews annually
              </li>
            </ul>

            <div class="button-row">
              <button class="primary-btn ant-btn ant-btn-default">
                <span>Start with Annual – Save 44%</span>
              </button>
            </div>
          </div>
        </div>
      </div>

      <!-- MONTHLY CARD -->
      <div class="pricing-col">
        <div class="pricing-card basic-card ant-card ant-card-bordered css-vrrzze">
          <div class="ant-card-body pricing-card-body">
            <div class="pricing-header">
              <h4 class="plan-type">Monthly</h4>
              <h3 class="price-text">$29/month</h3>
            </div>

            <hr class="card-divider" />

            <ul class="feature-list">
              <li class="feature-item">
                <span class="feature-dot"></span>Unlimited access
              </li>
              <li class="feature-item">
                <span class="feature-dot"></span>Cancel anytime
              </li>
              <li class="feature-item">
                <span class="feature-dot"></span>Renews monthly
              </li>
            </ul>

            <div class="button-row">
              <button class="primary-btn ant-btn ant-btn-default">
                <span>Try Monthly Plan</span>
              </button>
            </div>
          </div>
        </div>
      </div>

    </div>
  </div>
</section>

<style>
/* Layout */
.pricing-section {
  width: 100vw;
  height: auto;
}

.pricing-container {
  width: 100%;
  height: 100%;
  padding-top: 74px;
  padding-bottom: 108px;
  position: relative;
  z-index: 2;
  overflow: hidden;
}

/* Titles */
.pricing-title {
  color: #000;
  text-align: center;
  font-family: Inter;
  font-size: 46px;
  font-weight: 600;
  line-height: 120%;
  letter-spacing: -0.46px;
  margin-bottom: 0;
}

.pricing-subtitle {
  text-align: center;
  margin: 16px 0 38px;
  font-family: Inter;
  font-size: 18px;
  font-weight: 400;
  line-height: 140%;
  color: #3f3f3f;
}

/* Grid */
.pricing-row {
  margin-left: -16px;
  margin-right: -16px;
  margin-top: 24px;
  display: flex;
  justify-content: center;
}

.pricing-col {
  padding-left: 16px;
  padding-right: 16px;
  width: 406px;
  cursor: pointer;
  position: relative;
}

/* Popular badge */
.popular-badge {
  position: relative;
  top: 0;
  margin-left: 29px;
}

/* Card Styles */
.pricing-card {
border-color: rgba(179, 179, 179, 0.6);
    border-width: 1px;
    border-radius: 24px;
    box-shadow: rgba(0, 0, 0, 0.05) 0px 0px 8px;
    margin-top: 30px;
    padding: 24px 28px;
}

.featured-card {
  border-color: rgb(22, 119, 255);
  border-width: 2px;
}

.basic-card {
  border-color: rgba(179, 179, 179, 0.6);
  border-width: 1px;
  margin-top: 30px;
}

.pricing-card-body {
  padding: 0;
}

.pricing-header {
  margin-bottom: 15px;
}

/* Row with label + tag */
.pricing-label-row {
  display: flex;
  align-items: center;
}

/* Plan type */
.plan-type {
  font-family: Inter;
  font-weight: 600;
  font-size: 22px;
  color: #000;
  text-align: left;
  margin-bottom: 24px;
}

/* Discount badge */
.discount-tag {
  background-color: rgb(139, 255, 174);
  margin-top: 7px;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 24px;
  width: 103px;
  margin-left: 12px;
  border-radius: 50px;
}

.discount-icon {
  height: 16px;
  width: 16px;
}

.discount-text {
  color: #000;
  margin-left: 6px;
  font-family: Inter;
}

/* Pricing text */
.price-text {
  text-align: left;
  font-family: Inter;
  font-weight: 600;
  font-size: 28px;
  margin-bottom: 12px;
}

/* Pricecut image */
.pricecut-img {
  width: auto;
  height: auto;
}

/* Divider */
.card-divider {
  margin: 24px 0;
  border: 0.5px solid #d9d9d9;
  width: 100%;
}

/* Feature List */
.feature-list {
  text-align: left;
  padding: 0;
  list-style: none;
  margin: 0;
}

.feature-item {
  font-family: Inter;
  font-size: 15px;
  display: flex;
  align-items: center;
  margin-bottom: 10px;
}

.feature-dot {
  display: inline-block;
  width: 8px;
  height: 8px;
  background-color: rgb(33, 131, 237);
  margin-right: 12px;
  border-radius: 2px;
}

/* Buttons */
.button-row {
  text-align: center;
}

.primary-btn {
  width: 100%;
  height: 56px;
  margin-top: 14px;
  background-color: rgb(33, 131, 237);
  color: white;
  font-size: 16px;
  font-weight: 600;
  font-family: Inter;
  border-radius: 100px;
  padding: 13px 24px;
}
</style>
